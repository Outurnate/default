language: cpp
sudo: required
addons:
  apt:
    packages:
      - cmake
      - cmake-data
      - libcurl3-dev
    sources:
      - george-edison55-precise-backports
script: ./configure && cmake --build build
