language: cpp
sudo: required
addons:
  apt:
    packages:
      - cmake
      - cmake-data
      - libcurl4-openssl-dev
    sources:
      - george-edison55-precise-backports
script: ./configure && cmake --build build
